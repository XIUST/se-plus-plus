# Context Ingestion Feature

Purpose: UI workflow for submitting plain text or file-based study notes to the RAG ingestion API.

Exports:
- `ContextIngestionPanel` React component.

Rules:
- Keep this feature focused on source collection and ingestion status.
- Do not implement flashcard behavior here.
- Use the shared API client and shared contracts.

