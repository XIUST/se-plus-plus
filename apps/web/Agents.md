# Web App Module

Purpose: React frontend for Se++ study workflows.

Exports:
- Vite app entrypoint in `src/main.tsx`.
- Feature modules under `src/features`.

Rules:
- UI text must be English.
- Keep API calls behind typed client functions.
- Keep domain workflows in feature folders instead of a single large app component.

